package views

import (
	"fmt"
	"aedelfrid/portfolio-server/views/components"
)

templ Index() {
	<!DOCTYPE html>
	<html lang="en">
		@components.Header()
		@components.Hero()
		@components.Footer()
	</html>
	<script>
        function reloadPage() {
            setTimeout(function() {
                window.location.reload();
            }, 2000);
        }
    </script>
}
